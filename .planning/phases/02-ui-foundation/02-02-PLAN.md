---
phase: 02-ui-foundation
plan: 02
type: execute
depends_on: ["02-01"]
files_modified:
  - app/page.tsx
  - src/widgets/landing/hero-section.tsx
  - src/widgets/landing/features-section.tsx
  - src/widgets/landing/index.ts
---

<objective>
IdeaToPRD 랜딩 페이지의 Hero와 Features 섹션을 구현합니다. frontend-design 플러그인을 활용하여 고품질 UI를 생성합니다.

Purpose: 첫인상을 결정하는 Hero와 핵심 가치를 전달하는 Features 섹션으로 사용자의 관심을 끕니다.
Output: 반응형 Hero 섹션, 6개 Features 카드, app/page.tsx 통합
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-foundation/02-01-SUMMARY.md
@app/layout.tsx
@src/shared/ui/index.ts

**Tech stack available:**
- Next.js 16.1.1 (App Router)
- shadcn/ui (Button, Card, Input, Dialog)
- Tailwind CSS v4 + 커스텀 디자인 토큰
- React 19.2.3

**Established patterns:**
- FSD 아키텍처: widgets/에 페이지 조합 블록 배치
- 브랜드 색상: 보라색 계열 (--brand-primary)
- 타이포그래피: .heading-1~3, .body-large, .text-gradient

**Constraining decisions:**
- Phase 2-01: shadcn/ui 컴포넌트는 src/shared/ui/에서 import
- Phase 2-01: 브랜드 색상은 CSS 변수 사용

**frontend-design 플러그인 활용:**
- Hero/Features 섹션 구현 시 /frontend-design 명령 사용
- 프로젝트 컨셉: AI PRD 생성 도구 (창의적, 현대적, 전문적)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero 섹션 구현</name>
  <files>src/widgets/landing/hero-section.tsx, src/widgets/landing/index.ts</files>
  <action>
**frontend-design 플러그인 활용:**
`/frontend-design` 명령을 사용하여 Hero 섹션을 생성합니다.

**요청 프롬프트:**
```
IdeaToPRD - AI PRD 생성 SaaS의 Hero 섹션을 만들어주세요.

핵심 메시지:
- 메인 헤드라인: "아이디어 한 줄로 PRD 완성" (text-gradient 적용)
- 서브 헤드라인: "2-3일 걸리던 PRD 작성, 5분이면 충분합니다"
- CTA 버튼: "무료로 시작하기" (primary), "데모 보기" (secondary)

디자인 요구사항:
- 보라색 그라데이션 배경 또는 accent
- 현대적이고 깔끔한 느낌
- 반응형 (모바일 → 데스크톱)
- shadcn/ui Button 컴포넌트 사용
- 아이디어→PRD 변환을 시각화하는 간단한 일러스트/아이콘

기술 스택:
- Next.js 16 App Router
- Tailwind CSS v4
- shadcn/ui (import from '@/src/shared/ui')
- 타이포그래피: .heading-1, .body-large, .text-gradient 클래스 사용
```

**파일 생성:**
1. src/widgets/landing/hero-section.tsx
   - 'use client' 지시문 (Button 상호작용)
   - HeroSection 컴포넌트 export
   - 반응형 레이아웃 (flex-col → flex-row)
   - CTA 버튼 2개

2. src/widgets/landing/index.ts 생성:
   ```typescript
   export { HeroSection } from './hero-section';
   ```

**주의사항:**
- shadcn Button: `import { Button } from '@/src/shared/ui'`
- 브랜드 색상 사용: hsl(var(--brand-primary))
- .text-gradient 클래스로 헤드라인 강조
- 접근성: alt 텍스트, 적절한 heading 계층
  </action>
  <verify>
- `npm run build` 성공
- src/widgets/landing/hero-section.tsx 파일 존재
- Button 컴포넌트 정상 import
- `npm run lint` 패스
  </verify>
  <done>
- HeroSection 컴포넌트 생성
- 메인 헤드라인 + 서브카피 + CTA 버튼 2개
- 반응형 레이아웃 완료
  </done>
</task>

<task type="auto">
  <name>Task 2: Features 섹션 구현 및 랜딩 페이지 통합</name>
  <files>src/widgets/landing/features-section.tsx, src/widgets/landing/index.ts, app/page.tsx</files>
  <action>
**frontend-design 플러그인 활용:**
`/frontend-design` 명령을 사용하여 Features 섹션을 생성합니다.

**요청 프롬프트:**
```
IdeaToPRD - AI PRD 생성 SaaS의 Features 섹션을 만들어주세요.

6개 핵심 기능 (PRD plan.md 기반):
1. 즉시 PRD 생성 - "5분 만에 완성되는 PRD"
2. 5가지 템플릿 - "SaaS, Mobile, Marketplace, Extension, AI-Wrapper"
3. AI 기반 분석 - "Claude AI가 아이디어를 구조화"
4. 내보내기 - "Markdown 복사, PDF 다운로드"
5. PRD 히스토리 - "생성한 PRD를 언제든 다시 확인"
6. 유연한 가격 - "필요한 만큼만 크레딧 구매"

디자인 요구사항:
- 3x2 그리드 (데스크톱), 2x3 또는 1열 (모바일)
- 각 기능에 아이콘 (lucide-react 사용)
- shadcn/ui Card 컴포넌트 사용
- 호버 시 살짝 올라오는 애니메이션
- 섹션 제목: "왜 IdeaToPRD인가요?" (.heading-2)

기술 스택:
- Next.js 16 App Router
- Tailwind CSS v4
- shadcn/ui Card (import from '@/src/shared/ui')
- lucide-react 아이콘
```

**파일 생성/수정:**
1. lucide-react 설치 (필요시):
   - `npm install lucide-react`

2. src/widgets/landing/features-section.tsx
   - FeaturesSection 컴포넌트 export
   - 6개 feature 데이터 배열
   - Card 컴포넌트로 각 기능 렌더링
   - 반응형 그리드

3. src/widgets/landing/index.ts 업데이트:
   ```typescript
   export { HeroSection } from './hero-section';
   export { FeaturesSection } from './features-section';
   ```

4. app/page.tsx 수정:
   - 기존 Next.js 보일러플레이트 제거
   - HeroSection, FeaturesSection import 및 렌더링
   ```tsx
   import { HeroSection, FeaturesSection } from '@/src/widgets/landing';

   export default function Home() {
     return (
       <main className="min-h-screen">
         <HeroSection />
         <FeaturesSection />
       </main>
     );
   }
   ```

**주의사항:**
- Card 컴포넌트: `import { Card, CardHeader, CardTitle, CardDescription } from '@/src/shared/ui'`
- lucide-react 아이콘 lazy import 고려 (번들 사이즈)
- 호버 애니메이션: Tailwind transition-transform hover:-translate-y-1
  </action>
  <verify>
- `npm run build` 성공
- src/widgets/landing/features-section.tsx 파일 존재
- app/page.tsx가 HeroSection, FeaturesSection 렌더링
- lucide-react 설치됨 (package.json 확인)
- `npm run lint` 패스
  </verify>
  <done>
- FeaturesSection 컴포넌트 생성 (6개 기능 카드)
- 반응형 그리드 레이아웃
- app/page.tsx에 Hero + Features 통합
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hero 섹션과 Features 섹션이 포함된 랜딩 페이지</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Confirm Hero 섹션:
       - 메인 헤드라인 "아이디어 한 줄로 PRD 완성" 보임
       - 그라데이션 텍스트 스타일 적용됨
       - CTA 버튼 2개 존재 ("무료로 시작하기", "데모 보기")
       - 반응형: 모바일(375px)에서 세로 레이아웃
    4. Confirm Features 섹션:
       - "왜 IdeaToPRD인가요?" 제목 보임
       - 6개 기능 카드가 그리드로 표시
       - 각 카드에 아이콘, 제목, 설명 있음
       - 호버 시 카드가 살짝 올라가는 애니메이션
       - 반응형: 모바일에서 1열 또는 2열
    5. Check: 콘솔 에러 없음, 레이아웃 깨짐 없음
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] HeroSection, FeaturesSection 컴포넌트 존재
- [ ] app/page.tsx에서 정상 렌더링
- [ ] 반응형 레이아웃 동작 확인
- [ ] 사용자 UI 검증 완료
</verification>

<success_criteria>

- Hero 섹션: 헤드라인, CTA 버튼, 반응형 레이아웃
- Features 섹션: 6개 기능 카드, 그리드 레이아웃
- app/page.tsx 통합 완료
- 사용자 UI 검증 승인
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-foundation/02-02-SUMMARY.md`:

# Phase 02 Plan 02: Hero + Features Summary

**랜딩 페이지 Hero/Features 섹션 구현 완료**

## Accomplishments

- HeroSection 컴포넌트 (헤드라인, CTA, 반응형)
- FeaturesSection 컴포넌트 (6개 기능 카드)
- app/page.tsx 통합

## Files Created/Modified

- [파일 목록]

## Decisions Made

- [결정 사항]

## Issues Encountered

- [이슈 또는 None]

## Next Step

Phase 02-03 완료 대기 (CTA + Pricing + Footer)
</output>
