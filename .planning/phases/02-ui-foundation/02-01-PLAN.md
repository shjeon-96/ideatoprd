---
phase: 02-ui-foundation
plan: 01
type: execute
depends_on: []
files_modified:
  - components.json
  - app/globals.css
  - src/shared/lib/utils.ts
  - tailwind.config.ts
  - src/shared/ui/button.tsx
  - src/shared/ui/card.tsx
  - src/shared/ui/input.tsx
  - src/shared/ui/dialog.tsx
---

<objective>
shadcn/ui를 초기화하고 IdeaToPRD 프로젝트의 디자인 시스템을 구축합니다.

Purpose: 일관된 UI 컴포넌트와 디자인 토큰을 설정하여 Phase 2-7까지 사용할 기반을 마련합니다.
Output: shadcn/ui 설정, 기본 컴포넌트 4종, 디자인 토큰 정의
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@app/layout.tsx
@app/globals.css
@package.json

**Tech stack available:**
- Next.js 16.1.1 (App Router)
- Tailwind CSS v4
- React 19.2.3
- Vitest + Testing Library

**Established patterns:**
- FSD 아키텍처 (src/shared/, src/features/, src/entities/, src/widgets/)
- Path alias @/* 사용
- Prettier + ESLint 설정 완료

**Constraining decisions:**
- Phase 1: FSD 구조 채택 - 컴포넌트는 src/shared/ui/에 배치
- Phase 1: Tailwind CSS v4 사용 - @theme 디렉티브로 토큰 정의
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui CLI 초기화 및 기본 컴포넌트 설치</name>
  <files>components.json, src/shared/lib/utils.ts, app/globals.css, tailwind.config.ts</files>
  <action>
1. shadcn/ui CLI로 프로젝트 초기화:
   - `npx shadcn@latest init` 실행
   - 옵션: TypeScript, CSS variables 사용, src/ 디렉토리 사용
   - base color: neutral (깔끔한 SaaS 느낌)

2. 생성된 파일들을 FSD 구조에 맞게 조정:
   - components/ui/* → src/shared/ui/*로 이동
   - lib/utils.ts → src/shared/lib/utils.ts로 이동
   - components.json의 aliases 수정:
     ```json
     {
       "aliases": {
         "components": "@/src/shared/ui",
         "utils": "@/src/shared/lib/utils"
       }
     }
     ```

3. 기본 컴포넌트 설치 (FSD 경로에 직접):
   - `npx shadcn@latest add button card input dialog`

4. 각 컴포넌트를 src/shared/ui/index.ts에 re-export:
   ```typescript
   export { Button, buttonVariants } from './button';
   export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from './card';
   export { Input } from './input';
   export { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription } from './dialog';
   ```

**주의사항:**
- shadcn init이 생성하는 기본 경로(components/ui)를 사용하지 않고 FSD 경로로 변경
- tailwind.config.ts가 생성되면 Tailwind v4 설정과 충돌하지 않는지 확인
- CSS 변수가 app/globals.css에 올바르게 추가되었는지 확인
  </action>
  <verify>
- `npm run build` 성공
- src/shared/ui/button.tsx, card.tsx, input.tsx, dialog.tsx 파일 존재
- components.json의 aliases가 FSD 경로를 가리킴
- `import { Button } from '@/src/shared/ui'` 정상 작동
  </verify>
  <done>
- shadcn/ui 초기화 완료
- 4개 기본 컴포넌트 (Button, Card, Input, Dialog) 설치 및 FSD 경로 배치
- cn() 유틸리티 함수 src/shared/lib/utils.ts에 위치
  </done>
</task>

<task type="auto">
  <name>Task 2: 디자인 토큰 정의 (색상, 타이포그래피)</name>
  <files>app/globals.css, src/shared/ui/index.ts</files>
  <action>
1. app/globals.css에 IdeaToPRD 브랜드 색상 정의:
   - shadcn 기본 CSS 변수 유지
   - 커스텀 브랜드 색상 추가:
     ```css
     :root {
       /* 기존 shadcn 변수들... */

       /* IdeaToPRD 브랜드 색상 */
       --brand-primary: 262 83% 58%;      /* 보라색 계열 - 창의성, AI */
       --brand-primary-foreground: 0 0% 100%;
       --brand-secondary: 210 40% 96%;
       --brand-accent: 262 83% 68%;       /* 밝은 보라 */

       /* 그라데이션용 */
       --gradient-start: 262 83% 58%;
       --gradient-end: 316 73% 52%;       /* 핑크 보라 */
     }

     .dark {
       /* 다크모드 브랜드 색상 */
       --brand-primary: 262 83% 68%;
       --brand-primary-foreground: 0 0% 100%;
       --brand-secondary: 217 33% 17%;
       --brand-accent: 262 83% 78%;
     }
     ```

2. 타이포그래피 클래스 추가 (Tailwind @layer):
   ```css
   @layer components {
     .text-gradient {
       @apply bg-gradient-to-r from-[hsl(var(--gradient-start))] to-[hsl(var(--gradient-end))] bg-clip-text text-transparent;
     }

     .heading-1 {
       @apply text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight;
     }

     .heading-2 {
       @apply text-3xl md:text-4xl font-bold tracking-tight;
     }

     .heading-3 {
       @apply text-2xl md:text-3xl font-semibold;
     }

     .body-large {
       @apply text-lg md:text-xl text-muted-foreground;
     }
   }
   ```

3. src/shared/ui/index.ts 업데이트 - 타입 export 확인:
   ```typescript
   // 기존 컴포넌트 exports...

   // 타입 exports
   export type { ButtonProps } from './button';
   ```

**디자인 방향:**
- 보라색 계열: AI/창의성/혁신 이미지
- 그라데이션: 현대적이고 동적인 느낌
- 깔끔한 neutral 베이스: 가독성과 전문성

**주의사항:**
- shadcn이 생성한 기본 CSS 변수는 유지 (호환성)
- HSL 형식 사용 (shadcn 컨벤션)
- 다크모드 변수도 함께 정의
  </action>
  <verify>
- `npm run build` 성공
- app/globals.css에 브랜드 색상 변수 존재
- .text-gradient, .heading-1~3, .body-large 클래스 정의됨
- `npm run lint` 패스
  </verify>
  <done>
- IdeaToPRD 브랜드 색상 (보라색 계열) 정의
- 타이포그래피 유틸리티 클래스 4종 추가
- 라이트/다크 모드 색상 변수 완료
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] shadcn/ui 컴포넌트 4종 src/shared/ui/에 존재
- [ ] components.json aliases가 FSD 경로 사용
- [ ] 브랜드 색상 CSS 변수 정의됨
- [ ] 다크모드 색상 변수 정의됨
</verification>

<success_criteria>

- shadcn/ui 초기화 완료, FSD 구조와 통합
- Button, Card, Input, Dialog 컴포넌트 사용 가능
- 디자인 토큰 (색상, 타이포그래피) 정의 완료
- 빌드 및 린트 정상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-foundation/02-01-SUMMARY.md`:

# Phase 02 Plan 01: shadcn/ui + Design System Summary

**shadcn/ui 초기화 및 IdeaToPRD 디자인 시스템 구축 완료**

## Accomplishments

- shadcn/ui CLI 초기화, FSD 구조 통합
- 기본 컴포넌트 4종 설치 (Button, Card, Input, Dialog)
- IdeaToPRD 브랜드 색상 정의 (보라색 계열)
- 타이포그래피 유틸리티 클래스 추가
- 라이트/다크 모드 지원

## Files Created/Modified

- [파일 목록]

## Decisions Made

- [결정 사항]

## Issues Encountered

- [이슈 또는 None]

## Next Step

Ready for 02-02-PLAN.md and 02-03-PLAN.md (can run in parallel)
</output>
