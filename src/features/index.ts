/**
 * Features Layer (FSD)
 *
 * Business features that provide user value.
 * Examples: generate-prd, buy-credits, auth
 *
 * Rules:
 * - Can import from: entities, shared
 * - Cannot import from: app, pages, widgets, other features
 */

export {};
