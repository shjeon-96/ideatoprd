/**
 * Widgets Layer (FSD)
 *
 * Page-level composition blocks that combine features and entities.
 * Examples: Header, Sidebar, Footer, PageLayout
 *
 * Rules:
 * - Can import from: features, entities, shared
 * - Cannot import from: app, pages, other widgets
 */

export {};
