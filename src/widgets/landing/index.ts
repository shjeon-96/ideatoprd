/**
 * Landing Page Widgets (FSD)
 *
 * Page-level composition blocks for the landing page.
 * Each section is a self-contained widget with its own styling.
 */

export { HeroSection } from './hero-section';
export { FeaturesSection } from './features-section';
