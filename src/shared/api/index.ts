/**
 * Shared API Layer (FSD)
 *
 * API clients and request utilities.
 * Examples: supabaseClient, fetchWrapper, apiEndpoints
 *
 * Rules:
 * - Can import from: shared/lib only
 * - Handles all external communication
 */

export {};
