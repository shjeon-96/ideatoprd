/**
 * Shared Lib Layer (FSD)
 *
 * Utility functions, helpers, and common logic.
 * Examples: formatDate, cn (classnames), validators
 *
 * Rules:
 * - Cannot import from any other layer
 * - Must be pure functions or constants
 */

export {};
